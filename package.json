{
  "name": "react-form-capture",
  "version": "1.0.0",
  "description": "Quickly create components that can access values from inputs inside it like forms and form inputs. Simply add a container and replace useState with a new hook!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "hook",
    "useCapturedState",
    "forms",
    "inputs",
    "useState",
    "context"
  ],
  "author": "Marvin Lee",
  "license": "ISC",
  "dependencies": {
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  }
}
